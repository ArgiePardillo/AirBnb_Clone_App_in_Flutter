import 'package:flutter/material.dart';

class PromoteResort extends StatelessWidget {
  const PromoteResort({super.key});


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('PromoteResort '),
      ),
      body: const Center(
        child: Text('PromoteResort page!'),
      ),
    );
  }
}